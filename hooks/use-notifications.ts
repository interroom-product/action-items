import { useNotifications as useNotificationsContext } from "@/contexts/NotificationContext"

// Export the hook directly with the expected name
export const useNotifications = useNotificationsContext
